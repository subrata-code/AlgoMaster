# Backend placeholder

This folder is reserved for the future AlgoJourney API.

The current phase is **frontend-only**. Do not add server code, databases, or authentication here yet.

When ready, this directory will host the REST API that the frontend service layer will call.
